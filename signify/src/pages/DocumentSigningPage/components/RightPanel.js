import React from 'react';
import './SidePanel.css';

const RightPanel = () => {
  return (
    <div className="side-panel-inner">
      {/* Intentionally left blank per request — reserved for properties, fields, and settings */}
    </div>
  );
};

export default RightPanel;
