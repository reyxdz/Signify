import React from 'react';
import './SidePanel.css';

const LeftPanel = () => {
  return (
    <div className="side-panel-inner">
      {/* Intentionally left blank per request — reserved for tools, layers, thumbnails, etc. */}
    </div>
  );
};

export default LeftPanel;
