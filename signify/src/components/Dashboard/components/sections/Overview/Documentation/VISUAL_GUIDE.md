# Overview Page - Visual Architecture Guide

## 🎨 Component Tree

```
┌─────────────────────────────────────────────────────────────────┐
│                        Dashboard                                │
│  (activeSection === 'overview')                                 │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                    DashboardContent                             │
│  (renders Overview when activeSection === 'overview')           │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                   Overview.js (wrapper)                         │
│  (backward compatibility - imports OverviewPage)                │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────────┐
│                    OverviewPage ⭐ MAIN                         │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ State:                                                     │ │
│  │ - stats (totalDocuments, totalSignatures, ...)            │ │
│  │ - recentDocuments []                                       │ │
│  │ - activity []                                              │ │
│  │ - loading, error                                           │ │
│  │                                                            │ │
│  │ useEffect: Fetch from APIs                               │ │
│  │ - useFetchOverviewStats()                                │ │
│  │ - useFetchRecentDocuments()                              │ │
│  │ - useFetchActivity()                                      │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│     ┌────────────────────────────────────────────────────────┐  │
│     │ Statistics Grid                                        │  │
│     │ (display: grid, 4 columns)                             │  │
│     │                                                        │  │
│     │  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐             │  │
│     │  │Stat  │  │Stat  │  │Stat  │  │Stat  │             │  │
│     │  │Card1 │  │Card2 │  │Card3 │  │Card4 │             │  │
│     │  └──────┘  └──────┘  └──────┘  └──────┘             │  │
│     │                                                        │  │
│     │  <StatCard icon={FileText} value={12} label="Docs" /> │  │
│     │  <StatCard icon={PenTool} value={8} label="Sigs" />  │  │
│     │  <StatCard icon={Users} value={5} label="Shared" />  │  │
│     │  <StatCard icon={CheckCircle} value={92%} label="%" />│  │
│     └────────────────────────────────────────────────────────┘  │
│                                                                  │
│     ┌────────────────────────────────────────────────────────┐  │
│     │ Main Content Grid (2 columns on desktop)              │  │
│     │                                                        │  │
│     │  ┌─────────────────────────┐  ┌──────────────────┐   │  │
│     │  │  Recent Documents       │  │  Sidebar         │   │  │
│     │  │  (DocumentList)         │  │  ┌────────────┐  │   │  │
│     │  │                         │  │  │ Quick      │  │   │  │
│     │  │ ┌─────────────────────┐ │  │  │ Actions    │  │   │  │
│     │  │ │ Document 1          │ │  │  │            │  │   │  │
│     │  │ │ Contract_2026.pdf   │ │  │  │ [Upload]   │  │   │  │
│     │  │ │ 2 days ago │ Signed │ │  │  │ [Template] │  │   │  │
│     │  │ └─────────────────────┘ │  │  │ [Share]    │  │   │  │
│     │  │                         │  │  └────────────┘  │   │  │
│     │  │ ┌─────────────────────┐ │  │  ┌────────────┐  │   │  │
│     │  │ │ Document 2          │ │  │  │ Activity   │  │   │  │
│     │  │ │ Agreement_Final.doc │ │  │  │ Feed       │  │   │  │
│     │  │ │ 5 days ago │Pending │ │  │  │            │  │   │  │
│     │  │ └─────────────────────┘ │  │  │ Activity 1 │  │   │  │
│     │  │                         │  │  │ Activity 2 │  │   │  │
│     │  │ ┌─────────────────────┐ │  │  │ Activity 3 │  │   │  │
│     │  │ │ Document 3          │ │  │  │            │  │   │  │
│     │  │ │ Invoice_Q1.pdf      │ │  │  │ ...        │  │   │  │
│     │  │ │ 1 week ago │ Signed │ │  │  └────────────┘  │   │  │
│     │  │ └─────────────────────┘ │  │                  │   │  │
│     │  │                         │  │                  │   │  │
│     │  └─────────────────────────┘  └──────────────────┘   │  │
│     └────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 📊 Data Flow Diagram

```
                    User Login
                       │
                       ▼
          JWT Token stored in localStorage
                       │
                       ▼
    ┌─────────────────────────────────────────┐
    │  OverviewPage useEffect (on mount)      │
    │  Fetch data with JWT token              │
    └──────────┬──────────────────────────────┘
               │
       ┌───────┴────────┬────────────────┐
       ▼                ▼                ▼
  fetchStats()   fetchDocuments()   fetchActivity()
       │                │                │
       ▼                ▼                ▼
  /api/overview/   /api/documents/   /api/activity
    stats            recent
       │                │                │
       └────────────┬───┴────────────────┘
                    ▼
          Parse responses & setState()
                    │
        ┌───────────┼───────────┐
        ▼           ▼           ▼
    setStats()  setRecent   setActivity()
    Documents   Documents
        │           │           │
        └───────────┼───────────┘
                    ▼
          Render Components with state
                    │
        ┌───────────┴───────────┬──────────────┐
        ▼                       ▼              ▼
    StatCards            DocumentList    ActivityFeed
    (4 cards)            (5 items)       (10 items)
```

---

## 🔄 User Interaction Flow

```
User Sees Overview Page
        │
        ├─→ Views Statistics (Real-time data)
        │
        ├─→ Views Recent Documents
        │   └─→ Clicks on Document
        │       └─→ [Future: Open preview modal]
        │
        ├─→ Views Activity Feed
        │   └─→ Reviews recent actions
        │
        └─→ Uses Quick Actions
            ├─→ Clicks "Upload Document"
            │   └─→ [Future: File upload modal]
            │
            ├─→ Clicks "Use Template"
            │   └─→ [Future: Template selector]
            │
            └─→ Clicks "Share Document"
                └─→ [Future: Share dialog]
```

---

## 🎨 Layout Breakdown

### Desktop View (1024px+)
```
┌──────────────────────────────────────────────┐
│  Header: Welcome back, [Name]!               │
│  Subtitle: Here's what's happening today     │
├──────────────────────────────────────────────┤
│  Stats Grid (4 columns)                      │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐       │
│  │  12  │ │  8   │ │  5   │ │ 92%  │       │
│  │ Docs │ │ Sigs │ │Share │ │  %   │       │
│  └──────┘ └──────┘ └──────┘ └──────┘       │
├──────────────────────────────────────────────┤
│  Content Grid (2 columns)                    │
│                                              │
│  ┌─────────────────┐  ┌─────────────────┐   │
│  │                 │  │                 │   │
│  │  Recent Docs    │  │  Quick Actions  │   │
│  │  • Doc 1        │  │  [Upload]       │   │
│  │  • Doc 2        │  │  [Template]     │   │
│  │  • Doc 3        │  │  [Share]        │   │
│  │                 │  │                 │   │
│  ├─────────────────┤  ├─────────────────┤   │
│  │                 │  │                 │   │
│  │                 │  │  Activity Feed  │   │
│  │                 │  │  • Activity 1   │   │
│  │                 │  │  • Activity 2   │   │
│  │                 │  │  • Activity 3   │   │
│  │                 │  │                 │   │
│  └─────────────────┘  └─────────────────┘   │
└──────────────────────────────────────────────┘
```

### Mobile View (<768px)
```
┌──────────────────────────┐
│ Header                   │
├──────────────────────────┤
│ Stats Grid (1 column)    │
│ ┌────────────────────┐   │
│ │     12             │   │
│ │   Documents        │   │
│ └────────────────────┘   │
│ ┌────────────────────┐   │
│ │     8              │   │
│ │   Signatures       │   │
│ └────────────────────┘   │
│ ┌────────────────────┐   │
│ │     5              │   │
│ │    Shared          │   │
│ └────────────────────┘   │
│ ┌────────────────────┐   │
│ │    92%             │   │
│ │  Completion        │   │
│ └────────────────────┘   │
├──────────────────────────┤
│ Recent Documents         │
│ • Doc 1                  │
│ • Doc 2                  │
│ • Doc 3                  │
├──────────────────────────┤
│ Quick Actions            │
│ [Upload]                 │
│ [Template]               │
│ [Share]                  │
├──────────────────────────┤
│ Activity Feed            │
│ • Activity 1             │
│ • Activity 2             │
│ • Activity 3             │
└──────────────────────────┘
```

---

## 🔌 API Integration Architecture

```
┌─────────────────────────────────────────────┐
│         OverviewPage Component              │
│  (Consumes API)                             │
└────────────┬────────────────────────────────┘
             │
             │ (useCustomHooks)
             ▼
┌─────────────────────────────────────────────┐
│    useOverviewAPI Custom Hooks              │
│  • useFetchOverviewStats()                  │
│  • useFetchRecentDocuments()                │
│  • useFetchActivity()                       │
│  • useUploadDocument()                      │
└─────────┬──────────────────────────────────┘
          │ (HTTP Requests with JWT)
          ▼
┌─────────────────────────────────────────────┐
│    Backend API (server/index.js)            │
│  • GET /api/overview/stats                  │
│  • GET /api/documents/recent                │
│  • GET /api/activity                        │
│  • POST /api/documents/upload               │
│  • POST /api/documents/:id/share            │
│  • PATCH /api/documents/:id/status          │
└─────────┬──────────────────────────────────┘
          │ (Database Queries)
          ▼
┌─────────────────────────────────────────────┐
│    MongoDB Database                         │
│  • users collection                         │
│  • documents collection                     │
│  • activities collection                    │
│  • signatures collection                    │
└─────────────────────────────────────────────┘
```

---

## 📱 Responsive Breakpoint Logic

```
Screen Width
     │
     ├─ < 480px ──→ Small Mobile (1 column, full width)
     │
     ├─ 480-768px ──→ Mobile (1 column, adjusted spacing)
     │
     ├─ 768-1024px ──→ Tablet (1 column, larger padding)
     │
     └─ 1024px+ ──→ Desktop (2 columns, full layout)

Stats Grid Columns
     │
     ├─ < 480px ──→ 1 column
     ├─ 480-768px ──→ 1 column
     ├─ 768-1024px ──→ 2 columns
     └─ 1024px+ ──→ 4 columns (auto-fit)

Content Grid Columns
     │
     ├─ < 1024px ──→ 1 column (stack vertically)
     └─ 1024px+ ──→ 2 columns (side-by-side)
```

---

## 🔄 Error Handling Flow

```
API Call Made
     │
     ├─ Success ──→ Parse JSON ──→ setState() ──→ Render
     │
     └─ Failure ──→ Catch Error ──→ Log Error ──→ Show Banner
                                    │
                                    └─→ Use Mock Data ──→ Render with Fallback
```

---

## 🎯 State Management Hierarchy

```
OverviewPage (Parent)
│
├─ stats {
│   ├─ totalDocuments: number
│   ├─ totalSignatures: number
│   ├─ sharedDocuments: number
│   └─ completionRate: number
│
├─ recentDocuments: [
│   ├─ _id
│   ├─ name
│   ├─ fileName
│   ├─ status
│   ├─ modifiedAt
│   └─ ...
│
├─ activity: [
│   ├─ _id
│   ├─ type
│   ├─ title
│   ├─ description
│   ├─ details
│   ├─ timestamp
│   └─ ...
│
├─ loading: boolean
└─ error: string | null

      │
      └─ Passed as props to children
         ├─ StatCard (receiving stats)
         ├─ DocumentList (receiving recentDocuments, loading)
         ├─ ActivityFeed (receiving activity, loading)
         └─ QuickActions (receiving handlers)
```

---

## 📊 Performance Optimization Points

```
Optimization Opportunity
        │
        ├─ Parallel API Calls ✅
        │  Promise.all([fetchStats(), fetchDocs(), fetchActivity()])
        │
        ├─ Loading States ✅
        │  Skeleton loaders prevent layout shift
        │
        ├─ Memoization (Future)
        │  React.memo(StatCard) for expensive renders
        │
        ├─ useCallback (Future)
        │  Memoize handler functions
        │
        ├─ Code Splitting (Future)
        │  Lazy load Overview component
        │
        └─ Image Optimization (Future)
           Lazy load images, optimize formats
```

---

**Visual Guide Version**: 1.0.0
**Last Updated**: February 9, 2026
**Status**: ✅ Complete
